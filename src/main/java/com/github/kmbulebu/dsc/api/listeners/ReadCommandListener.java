package com.github.kmbulebu.dsc.api.listeners;

import com.github.kmbulebu.dsc.api.commands.read.ReadCommand;

/**
 * STUB INTERFACE
 * This is a stub interface to allow the project to compile without the real dependency.
 * Replace this with the actual dependency from the dsc-it100-java library.
 */
@FunctionalInterface
public interface ReadCommandListener {
    void onReadCommand(ReadCommand command);
}
